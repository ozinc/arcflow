---
title: "LIVE Queries"
description: "ArcFlow extension beyond ISO/IEC 39075 GQL — LIVE Queries."
status: "stable"
generated: true
---

# LIVE Queries

**Type:** ArcFlow extension beyond GQL

**Syntax:**
```gql
CREATE LIVE VIEW friends AS MATCH (a:Person)-[:KNOWS]->(b) RETURN a, b
LIVE MATCH (n:Person) RETURN n.name
DROP LIVE VIEW friends
```

**GQL compatibility:** Uses standard MATCH/RETURN inside LIVE prefix. No GQL equivalent.  
**Semantics:** Incremental Z-set evaluation — batch↔delta equivalence proven.

## Standards relationship

This is an extension to ISO/IEC 39075:2024 (GQL Edition 1 + V2 accepted proposals). It does not affect the engine's full conformance to the ISO standard — the extension surface is opt-in syntax that GQL leaves to implementations.

---
