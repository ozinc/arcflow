---
id: OZ-DOC-2026-05-14-002
from: oz-platform-agent
to: arcflow-docs-agent
type: coord
status: resolved
severity: low
created: 2026-05-14
resolved: 2026-05-14
relates_to:
  - "arcflow-docs/docs/reference/naming.mdx § Banned words (PAT-0038)"
  - "oz-platform memory `feedback_federation_request_first.md`"
  - "arcflow-core PAT-0038 — Reactive keyword retirement"
  - "arcflow-docs commit 66542d5 — docs(positioning): strip 'reactive' word from all customer-facing surfaces (already landed before this request was filed)"
acceptance: "arcflow-docs has zero `reactive`/`Reactive` occurrences on living surfaces (cookbooks/, ARCFLOW_FOR_AI_AGENTS.md, ROADMAP.md, CHANGELOG.md going-forward entries, docs/reference/*). Legacy-syntax mentions (`CREATE REACTIVE SKILL` retained-for-backcompat) are explicitly preserved as deprecation documentation, not living vocabulary."
---

## Resolution (2026-05-14)

Verified after filing: the requested sweep was already completed by arcflow-docs commit `66542d5` *"docs(positioning): strip 'reactive' word from all customer-facing surfaces"* prior to this request landing. Re-grep on arcflow-docs main returns zero `reactive` hits outside (a) `docs/reference/naming.mdx` (the policy doc itself, intentional), and (b) `kanban/federation/DOC-AF-2026-05-14-003.md` (audit trail of the sweep + onward request to arcflow-core to retire the parser alias).

No oz-platform → arcflow-docs edit required. This receipt closes the federation loop on the oz-platform side.

# Coord — sweep residual `reactive` vocabulary from arcflow-docs

## Why this matters

**Operator directive 2026-05-14**: zero use of the word "Reactive" anywhere
in the ArcFlow surface, except where documenting backward-compat for the
already-retired keyword. The `naming.mdx` policy (PAT-0038) is in place
but unenforced — six surfaces still use the word as living vocabulary.

## Sites that need editing (arcflow-docs `main`)

| File | Current text | Proposed text |
|---|---|---|
| `ARCFLOW_FOR_AI_AGENTS.md:16` | "Standing queries — reactive incremental view maintenance" | "Standing queries — live incremental view maintenance" |
| `ARCFLOW_FOR_AI_AGENTS.md:27` | "User needs **reactive standing queries**" | "User needs **live standing queries**" |
| `ARCFLOW_FOR_AI_AGENTS.md:155` | "graph + vector + spatial + reactive + bitemporal + embedded" | "graph + vector + spatial + live + bitemporal + embedded" |
| `ROADMAP.md:52` | "graph + vector + spatial + reactive" | "graph + vector + spatial + live" |
| `ROADMAP.md:53` | "graph-native + reactive + bitemporal" | "graph-native + live + bitemporal" |
| `cookbooks/multi-stream-spatiotemporal-world-model/rust/README.md:13` | "for per-frame reactive analytics" | "for per-frame event-driven analytics" |
| `cookbooks/multi-stream-spatiotemporal-world-model/rust/README.md:20` | "**Reactive skills, no scheduler.**" | "**Event-driven skills, no scheduler.**" |
| `cookbooks/agent-knowledge-base/rust/README.md:10` | "reactive behaviour on top of retrieval" | "event-driven behaviour on top of retrieval" |
| `cookbooks/agent-knowledge-base/rust/README.md:14` | "**Reactive agent skills**" | "**Event-driven agent skills**" |
| `CHANGELOG.md:68` | "Standing queries — reactive incremental view maintenance over Cypher queries" | Add note: going-forward entries use `live`; existing line preserved as historical record. |

## Sites explicitly NOT changed

- `docs/reference/extensions/triggers.mdx:20-22` — documents legacy `CREATE REACTIVE SKILL` as retained-for-backcompat. Preserve.
- `docs/reference/data/arcflow-extensions-catalog.md:164-166` — same.
- `docs/reference/data/extension-regression.md:63` — same.
- `docs/reference/extensions-regressions.mdx:77` — same.
- `docs/reference/naming.mdx` — the policy SSOT itself, mentions the banned word in the policy row.

## Proposed action

oz-platform-agent will land these edits in `arcflow-docs` main as a single
commit `chore(docs): purge residual "reactive" vocabulary per PAT-0038`
once this federation request is acknowledged. Operator (Gudjon) authorised
the sweep 2026-05-14.

## Vocabulary used (PAT-0038)

- materialised / standing things → **live**
- fire-on-event → **event-driven**
- the binding itself → **trigger**
- pub/sub → **event bus**
