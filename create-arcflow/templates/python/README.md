# {{PROJECT_NAME}}

An [ArcFlow](https://github.com/ozinc/arcflow)-based project. Embedded graph
database — no server needed.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .[dev]
python -m {{PACKAGE_NAME}}.main
```

Agent orientation lives in [`CLAUDE.md`](CLAUDE.md).
