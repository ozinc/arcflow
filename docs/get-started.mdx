---
title: "Get Started"
description: "Try ArcFlow in your browser, install it, or embed it in your app"
section: "get-started"
status: "stable"
---

# Get Started

ArcFlow is an embedded graph database. It runs in the browser, Node.js, Python, Rust, and Docker. No server needed.

## Try instantly — zero install

Open **[ArcFlow Engine](https://arcflow.dev/engine)** in your browser. Paste any Cypher or GQL query — both work. Persistent storage. No install, no signup.

```cypher
CREATE (alice:Person {name: 'Alice', age: 30})
CREATE (bob:Person {name: 'Bob', age: 35})
CREATE (a:Person {name: 'Alice'})-[:KNOWS]->(b:Person {name: 'Bob'})
MATCH (n:Person) RETURN n.name, n.age
CALL algo.pageRank()
```

## Install

```bash
curl -fsSL https://arcflow.dev/install | sh
```

Then:

```bash
arcflow --playground
```

## All paths

| Path | Time | Friction |
|---|---|---|
| [Browser](https://arcflow.dev/engine) | 0s | Zero — click and go |
| Binary | 60s | `curl ... \| sh && arcflow` |
| npm | 2 min | `npm install arcflow` |
| Docker | 30s | `docker run ghcr.io/ozinc/arcflow:latest` |
| CLI (coding agents) | instant | `arcflow query '...'` |
| MCP (cloud chat UIs) | 1 min | `npx arcflow-mcp` |

## Next

- [Quickstart](/docs/quickstart) — first query in 5 minutes
- [Installation](/docs/installation) — all install methods in detail
- [Project Setup](/docs/project-setup) — Express, testing, monorepo

### Guides

- [Data Quality](/guides/data-quality) — live DQ, batch==delta equivalence, pipeline prune
- [Code Intelligence](/guides/code-intelligence) — queryable codebase graph for coding agents
- [From SQL to GQL](/guides/sql-to-gql) — connect any PostgreSQL client to ArcFlow
- [Agent Integration](/guides/agent-integration) — CLI binary, MCP, and napi-rs access patterns
