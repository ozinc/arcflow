---
title: "PROFILE"
description: "Execute a query and show microsecond-level performance timing"
section: "worldcypher/functions"
order: 6
since: "0.22.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH s, d, r, z, s.reliability * d.confidence AS composite_trust, d.confidence * r.battery AS operational_score RETURN s.name AS sensor, s.type AS type, s.reliability AS reliability, d.method AS method, composite_trust, r.name AS robot, r.task AS task, r.battery AS battery, operational_score, z.name AS zone, z.hazard AS hazard ORDER BY composite_trust DESC"
---

# PROFILE

Execute with per-operator microsecond timing. Returns results plus performance breakdown for each stage.

## Syntax

```cypher
PROFILE <any query>
```

## Example

```cypher
PROFILE MATCH (n:Person) WHERE n.age > 25 RETURN n.name ORDER BY n.age
```

Returns the query results plus microsecond timing for scan, filter, project,
and sort stages.

## See Also

- [EXPLAIN](/worldcypher/functions/explain) — show plan without executing
