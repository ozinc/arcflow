---
title: "EXPLAIN"
description: "Inspect the execution plan for world model queries — understand how MATCH, CALL, and live queries are executed"
section: "worldcypher/functions"
order: 11
since: "0.7.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WHERE d.confidence > 0.88 AND s.reliability >= 0.90 WITH z.name AS zone, z.hazard AS hazard, count(r) AS entity_count, avg(r.battery) AS avg_battery, avg(s.reliability * d.confidence) AS composite_trust RETURN zone, hazard, entity_count, avg_battery, composite_trust, CASE WHEN composite_trust >= 0.90 THEN 'optimal' WHEN composite_trust >= 0.75 THEN 'nominal' ELSE 'degraded' END AS status ORDER BY composite_trust DESC"
---

# EXPLAIN

See the query plan without executing. Shows the compiled operator pipeline: scan, filter, project, sort steps.

## Syntax

```cypher
EXPLAIN <any query>
```

## Example

```cypher
EXPLAIN MATCH (n:Person) WHERE n.age > 25 RETURN n.name ORDER BY n.age
```

Returns the compiled operator pipeline: scan, filter, project, sort steps.

## See Also

- [WorldCypher Overview](/worldcypher) — all operators
