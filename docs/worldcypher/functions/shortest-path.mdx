---
title: "shortestPath"
description: "Find the shortest path between two world model entities — inline pattern syntax, composable with MATCH and confidence filters."
section: "worldcypher/functions"
order: 10
since: "0.6.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[occ:OCCUPIES]->(z:Zone) RETURN s.name AS path_origin, s.type AS sensor_type, s.reliability AS origin_reliability, d.method AS detection_method, d.confidence AS detection_confidence, r.name AS via_robot, r.task AS task, r.x AS robot_x, r.y AS robot_y, occ.entered_seq AS zone_entry_seq, z.name AS path_destination, z.hazard AS destination_hazard ORDER BY d.confidence DESC"
---

# shortestPath

```cypher
shortestPath (a:Person {name: 'Alice'}), (b:Person {name: 'Dave'})
```

BFS-based shortest path between two nodes. Returns the path as a sequence of nodes and relationships.

## Syntax

```cypher
shortestPath (a:Label {key: value}), (b:Label {key: value})
```

## See Also

- [MATCH variable-length paths](/worldcypher/query-syntax/match) — `*min..max` traversal
