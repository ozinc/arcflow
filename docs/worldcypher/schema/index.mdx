---
title: "Schema"
description: "Schema primitives for the world model — property indexes, vector indexes, full-text indexes, and UNIQUE constraints."
section: "worldcypher/schema"
order: 1
since: "0.10.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH z.name AS zone, z.hazard AS hazard, count(r) AS entity_count, count(s) AS sensor_count, avg(r.battery) AS avg_battery, avg(s.reliability * d.confidence) AS composite_trust RETURN zone, hazard, entity_count, sensor_count, avg_battery, composite_trust, CASE WHEN composite_trust >= 0.90 THEN 'schema-valid' WHEN composite_trust >= 0.75 THEN 'partial-coverage' ELSE 'schema-gap' END AS coverage_status ORDER BY composite_trust DESC"

---

# Schema

WorldCypher supports explicit schema management for indexes and constraints.

## Indexes

Speed up property lookups:

```cypher
CREATE INDEX ON :Person(name)
CREATE INDEX ON :Person(age)
DROP INDEX ON :Person(name)
```

List all indexes:

```cypher
CALL db.indexes()
```

## Constraints

Enforce data integrity:

```cypher
CREATE CONSTRAINT ON (n:Person) ASSERT n.email IS UNIQUE
DROP CONSTRAINT ON (n:Person) ASSERT n.email IS UNIQUE
```

List all constraints:

```cypher
CALL db.constraints()
```

## See Also

- [CREATE INDEX](/worldcypher/schema/indexes) — index management
- [UNIQUE Constraint](/worldcypher/schema/constraints) — uniqueness enforcement
