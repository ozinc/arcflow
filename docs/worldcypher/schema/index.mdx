---
title: "Schema"
description: "Schema primitives for the world model — property indexes, vector indexes, full-text indexes, and UNIQUE constraints."
section: "worldcypher/schema"
order: 1
since: "0.10.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH s, d, r, z, s.reliability * d.confidence AS composite_trust RETURN s.name AS sensor_entity, s.type AS sensor_schema, s.reliability AS reliability, d.confidence AS confidence, d.method AS method, composite_trust, r.name AS robot_entity, r.task AS robot_schema, r.battery AS battery, r.speed AS speed, r.x AS x, r.y AS y, z.name AS zone_entity, z.hazard AS zone_schema ORDER BY composite_trust DESC"
---

# Schema

WorldCypher supports explicit schema management for indexes and constraints.

## Indexes

Speed up property lookups:

```cypher
CREATE INDEX ON :Person(name)
CREATE INDEX ON :Person(age)
DROP INDEX ON :Person(name)
```

List all indexes:

```cypher
CALL db.indexes()
```

## Constraints

Enforce data integrity:

```cypher
CREATE CONSTRAINT ON (n:Person) ASSERT n.email IS UNIQUE
DROP CONSTRAINT ON (n:Person) ASSERT n.email IS UNIQUE
```

List all constraints:

```cypher
CALL db.constraints()
```

## See Also

- [CREATE INDEX](/worldcypher/schema/indexes) — index management
- [UNIQUE Constraint](/worldcypher/schema/constraints) — uniqueness enforcement
