---
title: "CASE WHEN"
description: "Conditional logic in world model queries — classify entities, bucket confidence scores, and branch on property values inline in RETURN or WITH."
section: "worldcypher/composition"
order: 3
since: "0.12.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH z.name AS zone, z.hazard AS hazard, count(r) AS entity_count, avg(r.battery) AS avg_battery, avg(s.reliability * d.confidence) AS composite_trust RETURN zone, hazard, entity_count, avg_battery, composite_trust, CASE WHEN composite_trust >= 0.90 THEN 'tier-1-optimal' WHEN composite_trust >= 0.75 THEN 'tier-2-nominal' WHEN composite_trust >= 0.60 THEN 'tier-3-marginal' ELSE 'tier-4-degraded' END AS operational_tier ORDER BY composite_trust DESC"

---

# CASE WHEN

```cypher
MATCH (n:Person) RETURN n.name,
  CASE WHEN n.age < 18 THEN 'minor'
       WHEN n.age < 65 THEN 'adult'
       ELSE 'senior' END AS category
```

Conditional expressions for computed values in RETURN clauses.

## Syntax

```cypher
CASE WHEN condition THEN result [WHEN condition THEN result]... [ELSE default] END
```

## Examples

### Null handling

```cypher
MATCH (n:Person) RETURN n.name,
  CASE WHEN n.email IS NOT NULL THEN 'has email' ELSE 'no email' END AS status
```

## See Also

- [coalesce()](/worldcypher/functions/built-in) — null fallback
- [RETURN](/worldcypher/query-syntax/return) — projection
