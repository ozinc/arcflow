---
title: "UNWIND"
description: "Expand a list into individual rows for per-element processing — pairs with collect() for batch world model operations."
section: "worldcypher/composition"
order: 2
since: "0.12.0"
status: "stable"
playground:
  query: |-
    UNWIND ['Assembly', 'Quality-Control', 'Logistics', 'Quarantine'] AS area_name
    MERGE (z:Zone {id: area_name, name: area_name, active: 1, hazard: 0.1, radius: 15.0})
    RETURN z.name, z.active, z.hazard, z.radius ORDER BY z.name

---

# UNWIND

```cypher
UNWIND ['Alice', 'Bob', 'Carol'] AS name CREATE (n:Person {name: name})
```

Expand a list into individual rows. Each element becomes its own row for downstream processing.

## Examples

### Expand a literal list

```cypher
UNWIND [1, 2, 3] AS item RETURN item
```

### Process collected results

```cypher
MATCH (n:Person) WITH collect(n.name) AS names UNWIND names AS name RETURN name
```

## See Also

- [WITH](/worldcypher/composition/with) — query chaining
- [COLLECT aggregation](/worldcypher/functions/aggregations) — collect into lists
