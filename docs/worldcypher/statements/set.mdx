---
title: "SET"
description: "Mutate properties on matched nodes"
section: "worldcypher/statements"
order: 8
since: "0.3.0"
status: "stable"
playground:
  seed: world
  query: |-
    MATCH (r:Robot {id: 'r1'})
    SET r.battery = 96, r.status = 'charging', r.task = 'standby', r.dock_seq = 201
    RETURN r.name, r.task, r.battery, r.status, r.dock_seq, r.x, r.y

---

# SET

```cypher
MATCH (n) WHERE n.name = 'Alice' SET n.age = 31
```

Update properties on matched nodes or relationships. Automatically updates the `updated_at` timestamp.

## Syntax

```cypher
MATCH (variable) WHERE predicate SET variable.key = value
```

## See Also

- [MATCH](/worldcypher/query-syntax/match) + [WHERE](/worldcypher/query-syntax/where) — select what to update
