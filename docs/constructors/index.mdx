---
title: "Constructors"
description: "WorldCypher operations for creating and executing relationship constructors"
section: "constructors"
order: 1
since: "0.2.0"
status: "stable"
playground:
  query: "MERGE (sk:Skill {id: 'zone-watch', name: 'ZoneWatch', version: 3, min_confidence: 0.82, radius: 25.0, active: 1}) MERGE (a1:Agent {id: 'agent-01', name: 'Scout-Alpha', role: 'observer', active: 1, zone: 'lab-a', priority: 1}) MERGE (a2:Agent {id: 'agent-02', name: 'Scout-Beta', role: 'observer', active: 1, zone: 'lab-b', priority: 2}) MERGE (s1:Sensor {id: 'cam-1', name: 'Cam-LabA', type: 'camera', reliability: 0.95, poll_hz: 25}) MERGE (s2:Sensor {id: 'lidar-1', name: 'Lidar-LabB', type: 'lidar', reliability: 0.91, poll_hz: 10}) MERGE (sk1:Skill {id: 'zone-watch'})-[:GOVERNS]->(a1:Agent {id: 'agent-01'}) MERGE (sk2:Skill {id: 'zone-watch'})-[:GOVERNS]->(a2:Agent {id: 'agent-02'}) MERGE (a1m:Agent {id: 'agent-01'})-[:MONITORS]->(s1:Sensor {id: 'cam-1'}) MERGE (a2m:Agent {id: 'agent-02'})-[:MONITORS]->(s2:Sensor {id: 'lidar-1'}) MATCH (sk:Skill)-[:GOVERNS]->(a:Agent)-[:MONITORS]->(s:Sensor) RETURN sk.name AS constructor, sk.version AS version, sk.min_confidence AS min_confidence, a.name AS agent, a.role AS role, a.priority AS priority, s.name AS sensor, s.type AS sensor_type, s.reliability AS reliability, s.poll_hz AS poll_hz ORDER BY a.priority"

---

# Constructor Operations

Constructors turn static graphs into self-building systems. Register rules, process nodes, and the engine materializes relationships with confidence scores and provenance -- automatically.

## Operations

| Operation | What it does |
|---|---|
| [CREATE CONSTRUCTOR](/constructors/create-constructor) | Register a new constructor |
| [PROCESS NODE](/constructors/process-node) | Execute constructors against nodes |
| [REPROCESS EDGES](/constructors/reprocess-edges) | Re-evaluate low-confidence edges |

---

## See Also

- [Using Constructors](/guides/using-constructors) — step-by-step guide with real examples
- [Observations & Evidence](/concepts/observations) — the observation model that constructors write to
- [Confidence & Provenance](/concepts/confidence-and-provenance) — how constructor edges carry confidence scores
- [Use Case: Knowledge Management](/use-cases/knowledge-management) — constructors in an entity extraction pipeline
