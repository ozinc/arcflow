---
title: "REPROCESS EDGES"
description: "Re-evaluate world model edges below a confidence threshold — delete stale edges and re-run their constructors for fresh, higher-quality relationships."
section: "constructors"
order: 4
since: "0.4.0"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot) WITH s, d.confidence AS confidence, d.composite_trust AS composite_trust, d.flagged AS flagged, r, s.reliability * d.confidence AS composite_trust SET d.flagged = composite_trust < 0.80, d.composite_trust = composite_trust MATCH (s:Sensor)-[d:DETECTED]->(r:Robot) RETURN s.name AS sensor, s.type, s.reliability, confidence, composite_trust, flagged, r.name AS entity, r.task, CASE WHEN composite_trust >= 0.90 THEN 'trusted - use directly' WHEN composite_trust >= 0.80 THEN 'verify - cross-check' ELSE 'flagged - manual review' END AS action ORDER BY composite_trust ASC"
---

# REPROCESS EDGES

Re-evaluate low-confidence edges when constructors improve. Deletes edges below a threshold and re-runs their constructors for fresh scores.

## Syntax

```cypher
REPROCESS EDGES WHERE confidence < <threshold>
```

## Example

```cypher
REPROCESS EDGES WHERE confidence < 0.6
```

This:
1. Finds all dynamic edges with confidence below 0.6
2. Deletes them
3. Re-executes the originating constructors
4. New edges get fresh confidence scores and updated provenance

## See Also

- [PROCESS NODE](/constructors/process-node)
- [Confidence & Provenance](/concepts/confidence-and-provenance)
