---
title: "REPROCESS EDGES"
description: "Re-evaluate world model edges below a confidence threshold — delete stale edges and re-run their constructors for fresh, higher-quality relationships."
section: "constructors"
order: 4
since: "0.4.0"
status: "stable"
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH z.name AS zone, z.hazard AS hazard, count(r) AS entity_count, avg(r.battery) AS avg_battery, avg(s.reliability * d.confidence) AS composite_trust RETURN zone, hazard, entity_count, avg_battery, composite_trust, CASE WHEN composite_trust >= 0.90 THEN 'trusted' WHEN composite_trust >= 0.80 THEN 'verify' ELSE 'flagged' END AS action ORDER BY composite_trust DESC"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH z.name AS zone, z.hazard AS hazard, count(r) AS entity_count, avg(r.battery) AS avg_battery, avg(s.reliability * d.confidence) AS edge_trust RETURN zone, hazard, entity_count, avg_battery, edge_trust, CASE WHEN edge_trust >= 0.90 THEN 'trusted' WHEN edge_trust >= 0.80 THEN 'marginal' ELSE 'stale' END AS reprocess_verdict ORDER BY edge_trust DESC"

---

# REPROCESS EDGES

Re-evaluate low-confidence edges when constructors improve. Deletes edges below a threshold and re-runs their constructors for fresh scores.

## Syntax

```cypher
REPROCESS EDGES WHERE confidence < <threshold>
```

## Example

```cypher
REPROCESS EDGES WHERE confidence < 0.6
```

This:
1. Finds all dynamic edges with confidence below 0.6
2. Deletes them
3. Re-executes the originating constructors
4. New edges get fresh confidence scores and updated provenance

## See Also

- [PROCESS NODE](/constructors/process-node)
- [Confidence & Provenance](/concepts/confidence-and-provenance)
