---
title: "Recipe: Temporal Queries"
description: "Time-travel queries over the world model — AS OF seq N snapshots, temporal decay scoring, and observation velocity metrics."
section: "recipes"
status: "stable"
playground:
  seed: world
  query: "MATCH (s:Sensor)-[d:DETECTED]->(r:Robot)-[:OCCUPIES]->(z:Zone) WITH z.name AS zone, z.hazard AS hazard, count(r) AS tracked_entities, avg(r.battery) AS avg_energy, avg(r.speed) AS avg_velocity, avg(s.reliability * d.confidence) AS observation_quality, min(s.reliability * d.confidence) AS min_observation_quality RETURN zone, hazard, tracked_entities, avg_energy, avg_velocity, observation_quality, min_observation_quality, CASE WHEN observation_quality >= 0.92 THEN 'high_fidelity' WHEN observation_quality >= 0.80 THEN 'operational' WHEN observation_quality >= 0.70 THEN 'degraded' ELSE 'unreliable' END AS temporal_confidence ORDER BY observation_quality DESC"

---

# Recipe: Temporal Queries

Query the graph at a specific point in time.

## Snapshot query (AS OF)

```typescript
// Query the graph as it was at a specific sequence checkpoint
const result = db.query("MATCH (n:Person) AS OF seq 100 RETURN n.name, n.age")
```

## Temporal decay

Apply exponential decay to rank recent data higher:

```typescript
const decayed = db.query("CALL temporal.decay(7, 0.01)")
// halfLife=7 days, floor=0.01
```

## Velocity (rate of change)

How fast is the graph changing?

```typescript
const v = db.query("CALL temporal.velocity(7)")  // last 7 days
console.log(v.rows[0].get('totalNodes'))
```

## Trajectory

Track an entity's changes over time:

```typescript
const trajectory = db.query("CALL temporal.trajectory()")
```

## Changes since checkpoint

```typescript
const changes = db.query("CALL db.changesSince")
```

---

## See Also

- [Temporal Queries](/temporal) — `AS OF`, `db.changesSince()`, and time-travel in depth
- [Event Sourcing](/event-sourcing) — every mutation as a temporal fact
- [Sync](/sync) — delta sync built on the same temporal log
