slug = "multi-stream-spatiotemporal-world-model"
title = "Multi-Stream Spatiotemporal World Model — 60 Hz Live Tracking + Synced Auxiliary Streams"
audience = ["python", "data-engineer", "ml", "agent"]
tags = ["spatial", "live"]
arcflow_apis = [
    "python.ArcFlow.execute",
    "python.ArcFlow.close",
    "python.QueryResult.row_count",
    "python.QueryResult.columns",
]
external_deps = [
    "pyarrow >= 16",
    "numpy >= 1.26",
]
runtime_minutes = 8
gpu_required = false
manifest_pin = "0.8.0"
data_provenance = "synthesized"
